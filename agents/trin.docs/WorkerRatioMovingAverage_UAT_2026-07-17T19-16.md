# Worker Ratio Moving Average UAT

Passed 42 unit tests, 7 integration tests, documentation tests, snapshots, and
debug build. Verified averaging of finalized assignments and retention when the
worker receives its next chunk.
