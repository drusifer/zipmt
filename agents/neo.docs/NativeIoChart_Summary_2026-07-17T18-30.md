# Native Multi-series I/O Chart

Replaced the custom text chart with one native Ratatui `Chart`. Input and output
use positive/negative Braille line datasets, MA10s uses distinct magenta line
datasets, and three muted scatter datasets provide dotted guides.

All 40 unit tests, 7 integration tests, documentation tests, Stream/Split
snapshots, and debug build pass.
