# Pipeline Flow Phase 3 Usability Test

Actual PTY streaming confirmed the left-to-right flow vocabulary, live one-based worker/chunk movement, visible Level/Throttle/Chunk/Workers cards, default 1 MiB chunk size, 4/4 workers, and level 9. CLI help also reports level 9. Approval is withheld once because the 80-column title truncates `RUNNING` to `RUNNIN`; shorten the decorative rule and add a header assertion.
