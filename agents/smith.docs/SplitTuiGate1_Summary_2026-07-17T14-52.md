# Split-mode TUI Uplift Gate 1

Approved all five stories. The plan improves visibility of system status with an aggregate summary and explicit WAIT/RUN/DONE sectors, uses one-based identities, and prevents false confidence by marking startup-fixed Partition and Pool controls as non-interactive.

Mirrored I/O parity reduces mode-switching cognitive load. Explicit visible ranges, overflow counts, and PgUp/PgDn paging preserve recognition at small sizes. Architecture must determine whether Compression Level can honestly affect unfinished Split work; if not, it must be fixed and skipped like Partition/Pool.

Gate 1: APPROVED.
